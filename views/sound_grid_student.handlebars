<div id="sound-grid" data-target="body" data-add="self">
	<h1>SOUND GRID! - STUDENT - {{groupID}}</h1>
	<h2 id="popupContent"></h2>
	{{> vowel_grid}}
	{{> group_scores}}
	{{> player_scores}}
	{{> ffa_scores}}

	<script>
		require(['app/view_sound_grid_student'], function (view_sound_grid_student) {
			view_sound_grid_student.start('{{roomID}}');
		});
	</script>
</div>